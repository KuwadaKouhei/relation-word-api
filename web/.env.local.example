# word-api endpoint (no trailing slash)
WORD_API_URL=https://54-95-178-15.nip.io

# API key issued by word-api (kept server-side only)
WORD_API_KEY=your-api-key-here

# optional: allow self-signed or IP-mismatch certs during dev
# NODE_TLS_REJECT_UNAUTHORIZED=0
